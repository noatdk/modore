# Build outputs
native/macos/build/
bridge/build/
build/

# Lazily fetched scripting engine deps (see `make fetch-luajit`).
third_party/luajit/

# CMake spill — defensive, in case anything ever configures in-tree
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
compile_commands.json

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp

# Node (Puppeteer pipeline test)
test/puppeteer/node_modules/

# ML classifier — training data, cached arrays, trained models
tools/data/
engine/models/*.mdl
engine/models/english_dict.txt

# UT dictionaries (large, fetched separately)
third_party/merge-ut-dictionaries/

# Python
__pycache__/
